# Development Guidelines

* Use modern camelCase for namings, except for routes
* Do not use Props or Args classes, use direct arguments
* Do not pass errors inside objects, handle them early
* Each interface or model should be in its own file
* Do not create barrel files but import directly
