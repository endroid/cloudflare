# Cloudflare Worker

## Package manager fixes

* Block package managers other than `pnpm`
* Prevent `pnpm install` from updating packages
* Update scripts to use `pnpm`

## GitHub Action

* Automatically deploy to Cloudflare
